12/25 ms_lexical_anylyze関数 終わり。
NULLチェックのテストを追加する必要あり。 -> 想定されないのでいりません。
メモリ確保失敗時の挙動が怪しい（今だとNULLを返しているだけ
とりあえず作ることを優先しているので、メモリの動的確保の構造体などは考慮していない。

memo
- variable,identifyは先頭がletterである必要があるが2文字目以降は数字も許容される。

疑問【解決済み）
t_token構造体のconst *charはフリーしなくていいのか
-> constでもfreeの際にキャストをすれば解放できる。

変数の$?以外の特殊パラメータについては考慮しないので、変数名として変換出来るようにする
-> &だけが特殊ケースになりそう。今はDECLINEDで返している。パーサーを作る時に考えたいかも。

・なんか
少しずつペアのやってることがわかってきて楽しい
test難しい。レクサーまでは耐えた。
